由bhw设计
# Lua 示例二：顺序与控制结构

## 示例1：顺序结构（伪代码）
```text
读取 a,b
sum = a + b
avg = sum / 2
输出 sum, avg
```
讲解：先定义输入输出，再写计算步骤。

## 示例2：控制结构（伪代码）
```text
if score >= 90 -> A
else if score >= 60 -> Pass
else -> Fail
```
讲解：分支顺序应从严格到宽松。
由bhw设计
