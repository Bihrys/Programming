// 由bhw设计
/*
第二部分 第九章 第一节 动态规划和基本模型
DP四步:
1) 定义状态
2) 写转移
3) 初始化
4) 提取答案

习题:
1) 台阶问题(每次走1或2步)。
2) 最小花费爬楼梯。

答案与讲解:
1) dp[i]=dp[i-1]+dp[i-2]。
2) 状态含义要先定义清楚。
*/
#include <stdio.h> 

int main(void) { 
    int n = 10; // 台阶数量
    long long dp[11] = {0}; // DP数组
    dp[0] = 1; // 初始化: 0阶有1种走法
    dp[1] = 1; // 初始化: 1阶有1种走法
    for (int i = 2; i <= n; ++i) dp[i] = dp[i - 1] + dp[i - 2]; // 状态转移
    printf("%lld\n", dp[n]); // 输出答案
    return 0; 
} 
// 由bhw设计
