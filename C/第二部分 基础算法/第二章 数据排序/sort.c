// 由bhw设计
/*
第二部分 第二章 数据排序
示例: 冒泡排序。
核心: 相邻比较交换, 每轮把最大值放到末尾。

习题:
1) 改成降序排序。
2) 增加“无交换提前结束”优化。

答案与讲解:
1) 比较符号改为a[j] < a[j+1]。
2) 每轮记录swapped标志。
*/
#include <stdio.h> 

int main(void) { 
    int a[6] = {5, 1, 4, 2, 8, 3}; // 待排序数组
    for (int i = 0; i < 5; ++i) { // 外层控制轮数
        for (int j = 0; j < 5 - i; ++j) { // 内层比较相邻元素
            if (a[j] > a[j + 1]) { // 若逆序则交换
                int t = a[j]; // 临时变量
                a[j] = a[j + 1]; // 前一个元素后移
                a[j + 1] = t; // 完成交换
            } 
        } 
    } 
    for (int i = 0; i < 6; ++i) printf("%d ", a[i]); // 输出排序结果
    printf("\n"); 
    return 0; 
} 
// 由bhw设计
