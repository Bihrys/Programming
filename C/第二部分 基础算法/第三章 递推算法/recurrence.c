// 由bhw设计
/*
第二部分 第三章 递推算法
示例: 斐波那契递推。
核心: 先定义状态, 再写转移。

习题:
1) 输出前20项斐波那契。
2) 用滚动变量把空间降到O(1)。

答案与讲解:
1) 循环打印f[i]。
2) 只保留前两项即可。
*/
#include <stdio.h> 

int main(void) { 
    int n = 10; // 目标项
    long long f[11] = {0}; // 递推数组
    f[1] = 1; // 初值1
    f[2] = 1; // 初值2
    for (int i = 3; i <= n; ++i) f[i] = f[i - 1] + f[i - 2]; // 转移方程
    printf("%lld\n", f[n]); // 输出第n项
    return 0; 
} 
// 由bhw设计
