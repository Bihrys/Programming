// 由bhw设计
/*
第二部分 第一章 高精度计算
示例: 大整数加法(字符串实现)。
核心: 从低位到高位逐位相加并处理进位。

习题:
1) 扩展实现大整数减法。
2) 扩展实现大整数乘法。

答案与讲解:
1) 先比较大小, 再逐位借位相减。
2) 用竖式乘法双循环累加。
*/
#include <stdio.h> 
#include <string.h> 

int main(void) { 
    char a[] = "123456789123456789"; // 第一个大整数
    char b[] = "999999999999999999"; // 第二个大整数
    char r[64] = {0}; // 存放结果
    int i = (int)strlen(a) - 1, j = (int)strlen(b) - 1, k = 0, carry = 0; // 下标与进位
    while (i >= 0 || j >= 0 || carry) { // 逐位计算直到无进位
        int x = (i >= 0) ? a[i--] - '0' : 0; // 取a当前位数字
        int y = (j >= 0) ? b[j--] - '0' : 0; // 取b当前位数字
        int s = x + y + carry; // 当前位求和
        r[k++] = (char)('0' + (s % 10)); // 保存当前位
        carry = s / 10; // 更新进位
    } 
    for (int p = k - 1; p >= 0; --p) printf("%c", r[p]); // 倒序输出结果
    printf("\n"); 
    return 0; 
} 
// 由bhw设计
