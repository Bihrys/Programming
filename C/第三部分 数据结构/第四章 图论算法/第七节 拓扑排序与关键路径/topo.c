// 由bhw设计
/*
第三部分 第四章 第七节 拓扑排序与关键路径
核心: DAG中按入度为0节点逐步删除。

习题:
1) 判断课程依赖是否有环。
2) 输出一种合法拓扑序。

答案与讲解:
1) 若无法处理完所有点则有环。
2) 入度归零时入队并输出。
*/
#include <stdio.h> 

int main(void) { 
    int order[4] = {0, 1, 2, 3}; // 示例拓扑序
    for (int i = 0; i < 4; ++i) printf("%d ", order[i]); // 输出拓扑排序结果
    printf("\n"); 
    return 0; 
} 
// 由bhw设计
