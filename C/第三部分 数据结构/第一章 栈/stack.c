// 由bhw设计
/*
第三部分 第一章 栈
核心: 后进先出(LIFO)。
应用: 括号匹配、表达式求值、函数调用栈。

习题:
1) 用栈判断括号是否匹配。
2) 设计支持min操作的最小栈。

答案与讲解:
1) 左括号入栈, 右括号匹配栈顶。
2) 额外维护一个最小值栈。
*/
#include <stdio.h> 

int main(void) { 
    int st[10], top = -1; // 数组模拟栈和栈顶下标
    st[++top] = 10; // 入栈10
    st[++top] = 20; // 入栈20
    printf("%d\n", st[top]); // 读栈顶
    top--; // 出栈一次
    printf("%d\n", st[top]); // 再读栈顶
    return 0; 
} 
// 由bhw设计
