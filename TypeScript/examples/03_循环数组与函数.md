由bhw设计
# TypeScript 示例三：循环数组与函数

## 示例1：循环与数组（伪代码）
```text
for i in [0..n-1]:
    读取 arr[i]
    更新最大值、最小值、总和
```
讲解：一次遍历做完统计，避免重复扫描。

## 示例2：函数与递归（伪代码）
```text
factorial(n):
    if n <= 1 return 1
    return n * factorial(n-1)
```
讲解：必须有终止条件，否则会栈溢出。
由bhw设计
